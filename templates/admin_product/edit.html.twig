{% extends 'base-admin.html.twig' %}

{% block title %}Editer Produit{% endblock %}

{% block body %}
    <h1>Editer Produit</h1>

    {{ include('admin_product/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_admin_product_index') }}" class="btn btn-success col-4 mt-3 mb-3">back to list</a>

    {{ include('admin_product/_delete_form.html.twig') }}
{% endblock %}
